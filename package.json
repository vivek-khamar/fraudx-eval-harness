{
  "name": "fraudx-claim-eval",
  "version": "1.0.0",
  "description": "Golden-claim promptfoo eval for the FraudX document-ingestion and report pipeline",
  "private": true,
  "scripts": {
    "pretest": "npm run generate:tests",
    "test": "node --test provider.test.js config-shape.test.js scripts/score-dashboard.test.js fraudx-client.test.js scripts/generate-tests-vars.test.js",
    "generate:tests": "node scripts/generate-tests-vars.js",
    "preeval": "npm run generate:tests",
    "eval:raw": "promptfoo eval -c promptfooconfig.yaml --no-cache --output results.json",
    "score": "node scripts/score-dashboard.js",
    "eval": "npm run eval:raw && npm run score",
    "mock-server": "node test/mock-server.js"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "promptfoo": "^0.122.0"
  },
  "engines": {
    "node": ">=20.16.0 <21 || >=22.3.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pdf-parse": "^2.4.5"
  }
}
